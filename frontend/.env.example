PORT=3000

# Scan API base URL. Leave unset to use the in-browser fake API (contract
# example responses). Set it at switchover (days 11-12) to hit the real API:
# NEXT_PUBLIC_API_URL=http://localhost:3001
